// This file contains the list of strings which need translation. It is necessary in order to create .pot files automatically.

_('magnifier');

// form.html
$('#farfalla_profile_chooser').append(_('Choose your profile...'));
$('#farfalla_activator').attr('value',_('Activate profile'));

// toolbar.html
$('#farfalla_loaded_plugins').append(_('Loaded plugin(s):'));
$('#change_profile').attr('value',_('Change profile'));